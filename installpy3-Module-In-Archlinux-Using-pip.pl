# Installing Python Modules in Arch Linux Using PIP

With both Python and Python-PIP installed, 
you should be able to easily install Python modules 
via PIP (Python Package Manager). 

#As demonstrated below:

- Examples

1. [How to Install Latest Python3 on Arch Linux](https://tinyurl.com/24xga6kq)

```python

$ python3 -m pip install module_name

$ python3 -m pip pyshorteners

```
2. [Getting Started with PIP on ArchLinux](https://linuxhint.com/install_pip_archlinux/)

# Archlinux Commands

ls /home/srhills -a

nmkinitcpio

localectl list-keymaps

sudo mount /dev/sdc1 /mnt/usb
lsblk -f
sudo mount /dev/sdb1 /mnt/usb
lsblk -f
cd /mnt/usb/
sudo pacman -S ranger
yay conky-lua-archers
sudo pacman -Syu
pacman -S thunar thunar-volman
